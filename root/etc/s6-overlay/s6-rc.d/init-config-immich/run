#!/usr/bin/with-contenv bash

# make folders
mkdir -p \
    /photos

# permissions
find /app/immich -path "*/node_modules" -prune -o -exec chown abc:abc {} +
lsiown -R abc:abc \
    /config \
    /photos
lsiown -R abc:abc /import 2>/dev/null