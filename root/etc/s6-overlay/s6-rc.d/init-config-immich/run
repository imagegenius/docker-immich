#!/usr/bin/with-contenv bash

# make folders
mkdir -p \
    /config/log/nginx \
    /photos \
    /run/immich

# permissions
find /app/immich -path "*/node_modules" -prune -o -exec chown abc:abc {} +
lsiown -R abc:abc \
    /config \
    /photos \
    /run/immich
