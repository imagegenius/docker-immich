# This file is automatically generated

name: Auto Release CI

on:
  workflow_dispatch:
  schedule:
    - cron: "20 0 * * 1-6"

jobs:
  check-for-updates:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3

      - name: Create Version
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: |
          git config --global user.email "github-actions@github.com"
          git config --global user.name "github-actions"
          VERSION=$(curl -sL https://api.github.com/repos/immich-app/immich/releases/latest | jq -r '.tag_name')
          OLD_VERSION=$(curl -sL "https://api.github.com/repos/hydazz/docker-immich/releases/latest" | jq -r '.tag_name')
          printf '{
               "tag_name": "'${VERSION}'",
               "target_commitish": "'${main}'",
               "name": "'${VERSION}'",
               "body": "Upgrading Immich '${OLD_VERSION}' to '${VERSION}'",
               "draft": false,
               "prerelease": false
          }' >releasebody.json
          curl -H "Authorization: token ${TOKEN}" -X POST https://api.github.com/repos/hydazz/docker-immich/releases -d @releasebody.json

